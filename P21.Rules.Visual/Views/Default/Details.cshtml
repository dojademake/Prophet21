@model P21.Extensions.Web.WebBusinessRule

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>BaseRule</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.XmlData)
        </dt>

        <dd>
            <div class="container mt-4">
                <span class="border p-3 bg-light">
                    <pre>
                    <code id="xmlContent">
                            @Html.DisplayFor(model => model.XmlData)
                        </code>     
                    </pre>
                </span>
            </div>

        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    @Html.ActionLink("Back to List", "Index")
</p>

@section Scripts {
    @Scripts.Render("~/bundles/beautify")
    <script>
        const xml = `<your-xml-content-here>`;
        document.getElementById('xmlContent').textContent = beautifyXml(xml);
    </script>
}
